# Local override for application secret (non-sensitive defaults)
apiVersion: v1
kind: Secret
metadata:
  name: acquisitions-app-secret
  labels:
    app: acquisitions-app
type: Opaque
stringData:
  DATABASE_URL: 'postgres://neon:neon@neon-local:5432/neondb'
  ARCJET_KEY: 'ajkey_local_dev'
