# ClusterIP Service exposing the app internally on port 80 -> container port 3000
apiVersion: v1
kind: Service
metadata:
  name: acquisitions-app
spec:
  type: ClusterIP
  selector:
    app: acquisitions-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
