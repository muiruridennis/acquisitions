# Sensitive application settings (placeholders in base)
# Provide real values via CI/secrets or overlay patches
apiVersion: v1
kind: Secret
metadata:
  name: acquisitions-app-secret
  labels:
    app: acquisitions-app
type: Opaque
stringData:
  # IMPORTANT: Replace these with real values or create the secret via kubectl
  # kubectl -n acquisitions create secret generic acquisitions-app-secret \
  #   --from-literal=DATABASE_URL=... --from-literal=ARCJET_KEY=...
  DATABASE_URL: "REPLACE_WITH_PROD_DATABASE_URL"
  ARCJET_KEY: "REPLACE_WITH_ARCJET_KEY"
